{
  "name": "sk-techverse-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Root scripts to run frontend + backend together for local development",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend/backend && npm install --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,magenta \"npm run dev --prefix frontend/backend\" \"npm start --prefix frontend\"",
    "backend": "npm run dev --prefix frontend/backend",
    "frontend": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix frontend/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
